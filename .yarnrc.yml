# Defines what linker should be used for installing Node packages (useful to enable the node-modules plugin), one of:
# pnp, pnpm and node-modules.
# See https://yarnpkg.com/configuration/yarnrc#nodeLinker
nodeLinker: node-modules

yarnPath: .yarn/releases/yarn-3.4.1.cjs
